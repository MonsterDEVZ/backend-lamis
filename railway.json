{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements_django.txt && python manage.py collectstatic --noinput --no-input"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && python manage.py create_admin && python manage.py load_test_data && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
